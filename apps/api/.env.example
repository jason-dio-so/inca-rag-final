# ============================================================================
# DB Contract (SSOT) - inca-RAG-final
# ============================================================================
#
# This is the single source of truth for database connection parameters.
# All database connections MUST use these environment variables.
#
# Constitutional: No hardcoded credentials in code.
#
# Docker container: inca_pg_step14
# - Runs on 0.0.0.0:5433->5432/tcp
# - POSTGRES_DB=inca_rag_final
# - POSTGRES_USER=postgres
# - POSTGRES_PASSWORD=postgres
# ============================================================================

# Database Host
# - Use 127.0.0.1 to avoid IPv6 (::1) issues
# - Default: 127.0.0.1
DB_HOST=127.0.0.1

# Database Port
# - Container maps 5433->5432
# - Default: 5433
DB_PORT=5433

# Database Name
# - Must match container POSTGRES_DB
# - Default: inca_rag_final
DB_NAME=inca_rag_final

# Database User
# - Must match container POSTGRES_USER
# - Default: postgres
DB_USER=postgres

# Database Password
# - Must match container POSTGRES_PASSWORD
# - Default: postgres
DB_PASSWORD=postgres

# ============================================================================
# Legacy Environment Variables (Deprecated)
# ============================================================================
# The following are kept for backward compatibility but should not be used:
# - POSTGRES_HOST (use DB_HOST)
# - POSTGRES_PORT (use DB_PORT)
# - POSTGRES_DB (use DB_NAME)
# - POSTGRES_USER (use DB_USER)
# - POSTGRES_PASSWORD (use DB_PASSWORD)
#
# apps/api/app/db.py will read both old and new variables,
# but new variables (DB_*) take precedence.
# ============================================================================

# ============================================================================
# Verification
# ============================================================================
# Run db_doctor.py to verify connection:
#   python apps/api/scripts/db_doctor.py
# ============================================================================
